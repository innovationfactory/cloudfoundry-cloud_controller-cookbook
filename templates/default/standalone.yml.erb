---
name: "standalone"
runtimes:
<%- node['cloudfoundry_dea']['runtimes'].each do |_, runtime| %>
<%- next unless node.recipes.include? runtime['cookbook'] %>
  - "<%= runtime['name'] %>":
      default: false
<%- end # runtimes.each %>
